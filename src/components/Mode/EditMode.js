import React from "react";
import {
  EditWrapper,
  ToolBar,
  Widgets,
} from "..";

function EditMode(props) {
  return (
      <EditWrapper>
          <ToolBar />
        <Widgets />
      </EditWrapper>
  );
}

export default EditMode;
